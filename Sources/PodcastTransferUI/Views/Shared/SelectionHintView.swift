import SwiftUI

struct SelectionHintView: View {
  var body: some View {
    VStack(alignment: .leading, spacing: 6) {
      Text("Select episodes to transfer")
        .font(.subheadline)
    }
  }
}
